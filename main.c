#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float n, porcentagem;
    scanf("%f", n);
    porcentagem = 0.10 * n;
    printf("%f", porcentagem);
	return 0;
}
